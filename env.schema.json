{
  "vars": [
    {
      "name": "TEMPLATE_UPSTREAM",
      "required": false,
      "description": "Override upstream owner/repo for template update checks"
    },
    {
      "name": "API_BASE_URL",
      "required": false,
      "description": "Child-app API base URL"
    },
    {
      "name": "LOG_LEVEL",
      "required": false,
      "description": "Child-app log verbosity"
    },
    {
      "name": "VITE_GOOGLE_CLIENT_ID",
      "required": false,
      "description": "Google OAuth client ID for desktop (see docs/GOOGLE_API_SETUP.md)"
    },
    {
      "name": "VITE_GOOGLE_CLIENT_SECRET",
      "required": false,
      "description": "Google OAuth client secret for native token exchange; never commit a live .env"
    },
    {
      "name": "VITE_GOOGLE_REDIRECT_URI",
      "required": false,
      "description": "Web/Vite OAuth redirect fallback; Tauri uses a one-shot loopback port"
    }
  ]
}
